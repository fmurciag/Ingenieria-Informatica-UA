<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--coordenadas del proyecto-->
    <groupId>ppss</groupId>
    <artifactId>drivers</artifactId>
    <version>1.0-SNAPSHOT</version>
    <!--Ruta: $HOME/.m2/repository/ppss/drivers/1.0-SNAPSHOT/drivers-1.0-SNAPSHOT.jar-->

    <!--Variables usadas en el build script-->
    <properties>
            <!--obligatorio a partir de maven 3-->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>11</maven.compiler.source>
        <maven.compiler.target>11</maven.compiler.target>
        <!--variable para el filtrado-->
        <filtrar.por>importantes,fase1</filtrar.por>
    </properties>

    <!--Librerias usadas en el codigo-->
    <dependencies>
        <!--implementar y compilarlos los tests accesos a org.junit.jupiter.api-->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.8.2</version>
            <scope>test</scope><!--donde se ejecytan los tets-->
        </dependency>

        <!--implementar y compilarlos los test parametrizados-->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>5.8.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!--Las goals que se ejecutan e el ciclo de vida-->
        <plugins>
        <!--compilamos los tests mediante la goal surefire:testCompile-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.9.0</version>
            </plugin>

            <!--Ejecutaremos los tests mediante la goal surefire:test-->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.0.0-M5</version>
                    <configuration><!--configuracion para crear un filtrado -->
                        <groups>${filtrar.por}</groups>
                        <excludedGroups>excluidos</excludedGroups>
                    </configuration>
                </plugin>
        </plugins>
    </build>
</project>